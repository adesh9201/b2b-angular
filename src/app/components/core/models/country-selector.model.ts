
export interface Country {
  name: string;        // Full name of the country, e.g., "India"
  short: string;       // Country short code, e.g., "IN"
  currency: string;    // Currency short code, e.g., "INR"
  flag: string;        // Flag image URL
}
