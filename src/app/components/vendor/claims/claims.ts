import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-claims',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './claims.html',
  styleUrl: './claims.css'
})
export class Claims {

}
